name: Auto Generate and Deploy

on:
  schedule:
    - cron: '0 23 */3 * *'
  workflow_dispatch:
    inputs:
      burst:
        description: '50記事一括生成を実行'
        required: false
        default: 'false'
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: Node.jsのセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: パッケージのインストール
        run: npm install

      - name: Gemini APIでコンテンツ生成
        if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          IS_BURST: ${{ github.event.inputs.burst }} 
        run: node scripts/generate-article.mjs

      - name: 生成された記事をコミットして保存
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          mkdir -p content/posts
          git add content/posts/
          if ! git diff --staged --quiet; then
            git commit -m "Auto-generate new article"
            git push
          else
            echo "追加された記事がないため、コミットをスキップしました"
          fi

      - name: Next.jsでビルド
        run: npm run build

      - name: SSH鍵のセットアップとデプロイ
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          FTP_HOST: ${{ secrets.SSH_HOST }}
          FTP_USER: ${{ secrets.SSH_USER }}
        run: |
          # 1. 鍵の配置
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          
          # 2. rsyncで一発デプロイ（★一番最後をご自身の3つ目のドメインに合わせてください）
          rsync -avz --delete -e "ssh -p 10022 -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa" ./out/ $FTP_USER@$FTP_HOST:~/conconsalsal.com/public_html/
